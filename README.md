# fan-completion
Code for completing a fan from its (n-1)-dimension skeleton. Inputs: the faces of the (n-1)-dimension skeleton, from the 1-dimension faces (rays) to the (n-1)-dimension faces. Outputs: the n-dimension cones.

$ \{ x^2\}$
